# ========== MyTwin Backend v12.2 – Complete Requirements ==========
# Hexagonal Architecture | Production Ready | Digital Twin OS

# --- API Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
python-dotenv==1.0.1

# --- AI & Models (Multi-Provider) ---
google-genai>=0.1.0          # Gemini (3 API Keys)
openai>=1.55.0               # Groq + OpenRouter (6 API Keys) via OpenAI client
groq>=0.13.0                 # Groq native client (optional fallback)
tiktoken>=0.5.0              # Token counting for cost optimization

# --- Database & Vector Memory ---
supabase>=2.6.0              # Main database & auth
pgvector>=0.2.0              # Semantic memory (pgvector extension)
numpy>=1.24.0                # Required by pgvector & Whisper

# --- Caching ---
cachetools>=5.3.0            # In-memory cache (Redis alternative)

# --- Security ---
cryptography>=41.0.7         # Encryption for sensitive data
PyJWT>=2.8.0                 # Local JWT decode (no Supabase call)

# --- Voice & Audio ---
edge-tts>=6.1.0              # Free TTS (Microsoft Neural voices)
vosk>=0.3.45                 # Lightweight STT (offline, no internet)

# --- External Services ---
httpx>=0.27.0                # Async HTTP client (tools, APIs)

# --- API Protection ---
slowapi>=0.1.9               # Rate limiting

# --- Monitoring & Observability ---
sentry-sdk[fastapi]>=2.0.0   # Error tracking & performance
psutil>=5.9.6                # System metrics (CPU, RAM)

# --- Utilities ---
tenacity>=8.2.0              # Retry logic
aiofiles>=23.0.0             # Async file operations
python-dateutil>=2.8.2       # Date handling
langdetect>=1.0.9            # Language detection
